/**
 * PostHog Provider (placeholder)
 * Para analytics - implementação futura
 */

'use client';

export function CSPostHogProvider({ children }: { children: React.ReactNode }) {
  // Implementação futura de analytics
  return <>{children}</>;
}
